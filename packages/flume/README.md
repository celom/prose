# flume

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build flume` to build the library.

## Running unit tests

Run `nx test flume` to execute the unit tests via [Vitest](https://vitest.dev/).
